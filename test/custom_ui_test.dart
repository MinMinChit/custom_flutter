import 'package:flutter_test/flutter_test.dart';

import 'package:custom_ui/gradient_linear_progress.dart';

void main() {
  test('adds one to input values', () {
    final calculator = GradientLinearProgressIndicator(progress: 0.9);
  });
}
